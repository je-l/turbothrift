version: '3'
services:
  client:
    build: client
    volumes:
      - ./client/src:/app/src
      - /app/node_modules
    ports:
      - 8080:8080
  server:
    build: server
    volumes:
      - ./server/src:/app/src
      - /app/node_modules
    ports:
      - 4000:4000

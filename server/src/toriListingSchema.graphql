type ToriItem {
  id: ID!
  title: String!
  url: String!
  priceEuroCents: Int!
  postedAt: String!
}

type Query {
  allToriQueries: [ToriItem!]!
}

type Mutation {
  addToriQuery(url: String!): String!
}
